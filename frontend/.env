# Vite API base used by the frontend at runtime (import.meta.env.VITE_API_BASE_URL)
# Set this to the address where the backend is reachable on your machine.
# Backend in this project is configured to listen on port 8081 by default.
VITE_API_BASE_URL=http://localhost:8081
